{
  "name": "tv-stations-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute tv-stations --local --file=./schema.sql",
    "db:migrate:prod": "wrangler d1 execute tv-stations --file=./schema.sql --remote"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.91.0"
  }
}
